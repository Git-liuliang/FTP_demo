这是一个FTP的小demo，基于python3.6开发

功能实现：
1.用户加密认证（通过对客户端连接服务端时利用秘钥判断）
2.允许同时多用户登陆
3.每个用户有自己的家目录，且只能访问自己的家目录
4.每个用户进行磁盘配额，每个用户的可用空间不同
5.允许用户在ftp server上随意切换目录
5.允许用户查看当前目录下文件
6.允许上传和下载文件，保证文件一致性
7.文件传输过程中显示进度条
8.附加功能：支持文件的断点续传


使用：

已创建两个用户：

用户名：密码
egon ：xinwei
alex ：alex123

执行start_server 开启服务端后，执行start_client开启客户端
默认ip为127.0.0.1 ，默认端口为8888，可以在conf中的margs文件中自定义